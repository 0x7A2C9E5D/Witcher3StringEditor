<Window x:Class="Witcher3StringEditor.Dialogs.Views.DictionaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        xmlns:vm="clr-namespace:Witcher3StringEditor.Dialogs.ViewModels"
        xmlns:sf="http://schemas.syncfusion.com/wpf"
        d:DataContext="{d:DesignInstance Type=vm:DictionaryDialogViewModel}"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        mc:Ignorable="d"
        Title="DictionaryDialog" Height="800" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="1" Margin="0,16,16,16" Command="{Binding AddDictionaryFromFileCommand}">
            <StackPanel Orientation="Horizontal">
                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Import}" />
                <TextBlock Margin="5,0,0,0" Text="导入字典文件" />
            </StackPanel>
        </Button>
        <sf:SfDataGrid NavigationMode="Row" RowHeight="35" ShowRowHeader="True" ItemsSource="{Binding Dictionaries}"
                       Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0"
                       AllowResizingColumns="True" Margin="16,0,16,16"
                       AutoGenerateColumns="False">
            <sf:SfDataGrid.Resources>
                <ResourceDictionary>
                    <!-- Custom style for row header cells to display row numbers -->
                    <Style TargetType="sf:GridRowHeaderCell">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="sf:GridRowHeaderCell">
                                    <Border
                                        x:Name="PART_RowHeaderCellBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                        <Grid>
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Text="{Binding RowIndex, RelativeSource={RelativeSource TemplatedParent}}"
                                                TextAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ResourceDictionary>
            </sf:SfDataGrid.Resources>
            <sf:SfDataGrid.Columns>
                <sf:GridTextColumn TextAlignment="Center" MappingName="SourceLanguage" HeaderText="源语言" />
                <sf:GridTextColumn TextAlignment="Center" MappingName="TargetLanguage" HeaderText="目标语言" />
                <sf:GridTemplateColumn HeaderText="删除" MappingName="Delete">
                    <sf:GridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button HorizontalAlignment="Center" CommandParameter="{Binding}"
                                    Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=DataContext.RemoveDictionaryCommand}">
                                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Delete}" />
                            </Button>
                        </DataTemplate>
                    </sf:GridTemplateColumn.CellTemplate>
                </sf:GridTemplateColumn>
            </sf:SfDataGrid.Columns>
        </sf:SfDataGrid>
    </Grid>
</Window>