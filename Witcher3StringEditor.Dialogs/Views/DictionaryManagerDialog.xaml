<Window x:Class="Witcher3StringEditor.Dialogs.Views.DictionaryManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        xmlns:vm="clr-namespace:Witcher3StringEditor.Dialogs.ViewModels"
        xmlns:sf="http://schemas.syncfusion.com/wpf"
        xmlns:c="clr-namespace:Witcher3StringEditor.Dialogs.Converters"
        xmlns:l="clr-namespace:Witcher3StringEditor.Locales;assembly=Witcher3StringEditor.Locales"
        d:DataContext="{d:DesignInstance Type=vm:DictionaryManagerDialogViewModel}"
        Title="{I18N {x:Static l:LangKeys.DictionaryDialogTitle}}" 
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        Closed="DictionaryDialog_OnClosed"
        mc:Ignorable="d" Width="1200" Height="800" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="1" Margin="0,16,16,16" Command="{Binding ImportDictionaryCommand}">
            <StackPanel Orientation="Horizontal">
                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Import}" />
                <TextBlock Margin="5,0,0,0" Text="{I18N {x:Static l:LangKeys.ImportDictionary}}" />
            </StackPanel>
        </Button>
        <sf:SfDataGrid NavigationMode="Row" RowHeight="35" ShowRowHeader="True"
                       ItemsSource="{Binding PagedSource, ElementName=SfDataPager}"
                       Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0"
                       AllowResizingColumns="True" Margin="16,0"
                       AutoGenerateColumns="False">
            <sf:SfDataGrid.Resources>
                <ResourceDictionary>
                    <!-- Custom style for row header cells to display row numbers -->
                    <Style TargetType="sf:GridRowHeaderCell">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="sf:GridRowHeaderCell">
                                    <Border
                                        x:Name="PART_RowHeaderCellBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                        <Grid>
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Text="{Binding RowIndex, RelativeSource={RelativeSource TemplatedParent}}"
                                                TextAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <c:PathToDisplayNameConverter x:Key="PathToDisplayNameConverter" />
                </ResourceDictionary>
            </sf:SfDataGrid.Resources>
            <sf:SfDataGrid.Columns>
                <sf:GridTextColumn TextAlignment="Center" MappingName="FilePath"
                                   HeaderText="{I18N {x:Static l:LangKeys.FileName}}">
                    <sf:GridTextColumn.ValueBinding>
                        <!-- ReSharper disable once Xaml.BindingWithContextNotResolved -->
                        <Binding Path="Path" Converter="{StaticResource PathToDisplayNameConverter}" />
                    </sf:GridTextColumn.ValueBinding>
                </sf:GridTextColumn>
                <sf:GridTextColumn TextAlignment="Center" MappingName="SourceLanguage.NativeName"
                                   HeaderText="{I18N {x:Static l:LangKeys.SourceLanguage}}" >
                </sf:GridTextColumn>
                <sf:GridTextColumn TextAlignment="Center" MappingName="TargetLanguage.NativeName"
                                   HeaderText="{I18N {x:Static l:LangKeys.TargetLanguage}}" />
                <sf:GridTextColumn TextAlignment="Center" MappingName="TermCount"
                                   HeaderText="{I18N {x:Static l:LangKeys.TermCount}}" />
                <sf:GridTemplateColumn HeaderText="{I18N {x:Static l:LangKeys.Delete}}" MappingName="Delete">
                    <sf:GridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button HorizontalAlignment="Center" CommandParameter="{Binding}"
                                    Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=DataContext.RemoveDictionaryCommand}">
                                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Delete}" />
                            </Button>
                        </DataTemplate>
                    </sf:GridTemplateColumn.CellTemplate>
                </sf:GridTemplateColumn>
            </sf:SfDataGrid.Columns>
        </sf:SfDataGrid>
        <sf:SfDataPager PageSize="30" Grid.Row="2" Grid.ColumnSpan="2" Grid.Column="0" HorizontalAlignment="Center"
                        VerticalContentAlignment="Center" Margin="0,4" x:Name="SfDataPager"
                        Source="{Binding Dictionaries}" />
    </Grid>
</Window>